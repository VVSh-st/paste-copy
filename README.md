<p align="center">
  <img src="logo.svg" width="80" alt="paste\copy logo">
</p>

# paste\copy

<!-- GIF / screenshot сюда -->

Браузерный редактор промптов для работы с LLM. Собирай сложные промпты из блоков, копируй в один клик, не теряй версии.

## Зачем

Потому что копипастить промпт из заметок в чат — это боль. А когда промпт из 10 блоков и ты меняешь один — боль двойная.

paste\copy позволяет:
- Разбить промпт на логические блоки (роль, контекст, задача, ограничения)
- Включать/выключать отдельные блоки из итогового текста
- Копировать собранный промпт одной кнопкой
- Хранить несколько промптов на вкладках

## Быстрый старт

1. Открой `index.html` в браузере
2. Создай вкладку (`+`)
3. Добавь блоки (`+ Добавить блок`)
4. Напиши промпт
5. Нажми «Скопировать превью» → вставь в чат с LLM

Сервер не нужен. Установка не нужна. Python не нужен.

## Возможности

### Блоки

| Тип | Описание |
|-----|----------|
| 📝 Текст | Основной блок с Markdown-превью |
| 📌 Sticky | Цветная заметка (не в превью) |
| ☑️ TODO | Чеклист с подзадачами |
| 📊 Таблица | Структурированные данные |
| 🧩 Переменные | `{{name}}` — меняешь一处, обновляется везде |
| 🧱 Сниппеты | Быстрые команды и шаблоны |

### Инструменты

- **Переводчик** — перевод прямо в блоке (Google, Microsoft, Tencent)
- **AI-трансформация** — выдели текст → попроси LLM изменить
- **Текстовый линтер** — убирает мусор без нейросетей (пробелы, невидимые символы, пунктуация)
- **Форматирование текста** — жирный, курсив, заголовки, списки, код — одной кнопкой
- **Текстовый экспандер** — сохраняй часто вводимые фразы, вставляй по клику
- **Автодополнение** — подсказки при наборе (работает из Gist)
- **Проверка орфографии** — Yandex Speller прямо в редакторе
- **Визуальный Diff** — сравнивай версии с подсветкой изменений
- **Якоря** — закладки на нужные места в тексте
- **Prompt Loom** — сниппеты и переменные через `\`
- **QR-код** — генерация из текста (размер 1–12px, несколько стилей)
- **Генератор карт** — Mindmap и Flowchart из текста
- **Счётчик слов** — статистика по тексту
- **Эмодзи-пикер** — быстрый вставка эмодзи
- **Тренажёр клавиатуры** — практика набора с фильтрацией проблемных клавиш
- **Умные подсказки** — анализ текста и рекомендации
- **Детекторы качества** — оценка промпта по критериям
- **Граф проекта** — связанные заметки и навигация
- **Память пользователя** — LLM запоминает контекст между сессиями
- **Ninja Cursor** — анимированный курсор при копировании

### Синхронизация

- **GitHub Gist** — Push/Pull между устройствами
- **Снапшоты** — история версий с возможностью отката и визуальным Diff
- **Локальный бэкап** — автоматическое сохранение

### Визуал

- **Ember** — анимированный угёк в角落е, реагирует на编辑 и копирование
- **Таймер** — обратный отсчёт / секундомер с визуальной индикацией

### LLM-интеграция

- Мини-чат — спроси модель не покидая редактор
- Оценка промпта — 5 критериев с анимированной шкалой
- Сжатие токенов — убери лишнее без потери смысла
- Автозаголовок — LLM придумает название для блока

## Горячие клавиши

| Комбинация | Действие |
|------------|----------|
| `Ctrl + S` | Сохранить |
| `Ctrl + Z` / `Y` | Отмена / Повтор |
| `Ctrl + F` | Поиск и замена (по всем вкладкам) |
| `F3` / `Shift + F3` | Следующее / предыдущее совпадение |
| `Ctrl + T` | Новая вкладка |
| `Ctrl + W` | Закрыть вкладку |
| `Ctrl + D` | Дублировать вкладку |
| `Ctrl + Shift + C` | Копировать превью |
| `Ctrl + Shift + 1` | Установить якорь |
| `Ctrl + Shift + 2` | Навигация по якорям |
| `Ctrl + Shift + 3` | Удалить все якоря |
| `Alt + L` | Мини-чат |
| `Ctrl + Enter` | Отправить в мини-чате |
| `\` | Prompt Loom palette (в текстовом поле) |
| `F2` | Справка |
| `Esc` | Закрыть панели, меню, справку |

## Технологии

- Vanilla JS (без фреймворков)
- CSS (без препроцессоров)
- HTML
- marked.js + highlight.js (CDN)

Всё работает в браузере. Данные хранятся в localStorage + GitHub Gist.

## Лицензия

MIT
