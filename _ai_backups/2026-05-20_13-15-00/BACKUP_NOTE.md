# Backup note

Файл затронут в этой сессии:
- llm-core.js

Перед правкой были прочитаны релевантные участки через grep/read и локальная документация Cherry API через Browser/Fetch.

Важно: попытка сделать полную копию через Python/shutil из MCP-среды не получила доступа к E:\\Cherry_studio (Pyodide sandbox FileNotFoundError). Browser MCP смог прочитать исходный llm-core.js через file:// до правок, но в доступных инструментах не было прямой native copy операции.

Изменения точечные и проверены через read/grep/Browser MCP. Для восстановления при необходимости используйте предыдущие полные backup-папки проекта или историю VCS/редактора.
