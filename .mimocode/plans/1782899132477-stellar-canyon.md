# План: + чеклист в Тезаурус

## Обзор

Добавить новый пункт "+ чеклист" в меню Тезаурус. При клике — выделенный текст (или весь блок) отправляется в LLM с промптом на создание TODO-чеклиста. Результат добавляется в блок "Чеклист" на свободную вкладку.

---

## Ключевые находки из исследования

### Уже существующий тип блока `todo`
- `state.js:421` — `type === 'todo'`, title='Чеклист', icon='☑️'
- 5 подвкладок (не 20 как у текстовых)
- Каждая подвкладка: `{ label, name: '', items: [] }`, items: `{ id, text, done }`
- Рендер: `renderTodoBody(b, body)`, `createTodoItem(b, sub, item, idx, items)`
- CSS: `.todo-item`, `.todo-checkbox`, `.todo-text`, `.todo-delete`, `.todo-add-btn`

### Меню Тезаурус (blocks.js)
- Массив меню: строки 1856-1862
- `_thesaurusModeLabels` на строке 1852
- Диспетчер кликов: строки 1899-1909
- Долгий клик (400ms) → dropdown `.translate-dropdown`

### LLM вызов (llm-features.js)
- `_executeThesaurusMode` switch: строки 704-711
- `_LLMCore.request()` — основной вызов
- Промпты: `llm-core.js:1795-1832` (BUILTIN_PROMPTS)
- `getPrompt(key, vars)` с интерполяцией `{var}`

---

## Реализация

### 1. Промпт для LLM (`llm-core.js`)

Добавить в `BUILTIN_PROMPTS`:
```
thesaurus_checklist: "Создай чеклист (задачи) из текста. Верни ТОЛЬКО список задач, каждая на новой строке, начиная с тире. Без комментариев, без заголовков, без markdown-разметки.\n\nТекст:\n{text}"
```

Добавить в `PROMPT_META`:
```
thesaurus_checklist: { group: 'Служебные', vars: ['text'] }
```

### 2. Новый пункт меню (`blocks.js`)

В `_thesaurusModeLabels` (строка 1852) добавить:
```js
checklist: '+ чеклист'
```

В массив меню `_buildThesaurusMenu()` (строки 1856-1862) добавить:
```js
{ id: 'checklist', label: '+ чеклист' }
```

### 3. Обработчик в `_executeThesaurusMode` (`llm-features.js`)

В switch (строка 704) добавить case:
```js
case 'checklist':
  _thesaurusChecklistAtBlock(blockId);
  return;
```

### 4. Новая функция `_thesaurusChecklistAtBlock` (`llm-features.js`)

```js
async function _thesaurusChecklistAtBlock(blockId) {
  const tab = State.getActive();
  if (!tab) return;
  const b = tab.blocks.find(x => x.id === blockId);
  if (!b) return;

  // Получаем текст: выделение или весь блок
  const ta = document.querySelector(`[data-block-id="${blockId}"] textarea`);
  const text = ta?.value?.slice(ta.selectionStart, ta.selectionEnd).trim()
    || ta?.value?.trim() || '';
  if (!text) { Toast.show('Нет текста', 'info'); return; }

  // LLM запрос
  Toast.show('Создаю чеклист...', 'info');
  const prompt = _LLMCore.getPrompt('thesaurus_checklist', { text: text.slice(0, 4000) });

  let result;
  try {
    result = await _LLMCore.request({
      messages: [{ role: 'user', content: prompt }],
      stream: false, maxTokens: 1000, featureTag: 'checklist',
    });
  } catch { Toast.show('Ошибка LLM', 'error'); return; }

  if (!result?.trim()) { Toast.show('Нет результата', 'info'); return; }

  // Парсим ответ → массив строк
  const items = result.trim().split('\n')
    .map(l => l.replace(/^[-–—*•]\s*/, '').replace(/^\d+[\.\)]\s*/, '').trim())
    .filter(Boolean);

  if (!items.length) { Toast.show('Чеклист пуст', 'info'); return; }

  // Ищем или создаём блок "Чеклист"
  let todoBlock = tab.blocks.find(x => x.type === 'todo');

  if (!todoBlock) {
    todoBlock = State.makeBlock('Чеклист', '☑️', 0, '', 'todo');
    tab.blocks.push(todoBlock);
  }

  // Ищем свободную подвкладку (пустые items)
  let freeSubtab = todoBlock.subtabs.find(s => !s.items.length);

  if (!freeSubtab) {
    // Нет свободных — показать выбор
    _showChecklistSubtabPicker(todoBlock, items, text);
    return;
  }

  // Заполняем подвкладку
  freeSubtab.name = text.slice(0, 30) + (text.length > 30 ? '...' : '');
  freeSubtab.items = items.map(t => ({
    id: crypto.randomUUID?.().slice(0, 8) || String(Date.now()),
    text: t, done: false
  }));

  State.update(() => {});  // триггер emit → Blocks.render()
  Toast.show(`Чеклист: ${items.length} пунктов`, 'success');
}
```

> **Важно:** `State.update(() => {})` с пустым колбэком — это хак для триггера `emit()`, который вызывает `Blocks.render()`. Фактически мы мутируем объект `todoBlock` напрямую (он по ссылке), а `update` лишь запускает persist + re-render.

### 5. Выбор подвкладки при отсутствии свободных

Когда все 5 подвкладок заняты — показать popup с выбором:

```js
function _showChecklistSubtabPicker(todoBlock, items, text) {
  const overlay = document.createElement('div');
  overlay.className = 'checklist-picker-overlay';
  overlay.innerHTML = `
    <div class="checklist-picker">
      <div class="checklist-picker-title">Все вкладки заняты</div>
      <div class="checklist-picker-subtitle">Какую заменить?</div>
      ${todoBlock.subtabs.map((s, i) => `
        <button class="checklist-picker-item" data-idx="${i}">
          <span class="checklist-picker-name">${s.name || 'Вкладка ' + (i + 1)}</span>
          <span class="checklist-picker-count">${s.items.length} пунктов</span>
        </button>
      `).join('')}
      <button class="checklist-picker-cancel">Отмена</button>
    </div>`;
  document.body.appendChild(overlay);

  overlay.querySelectorAll('.checklist-picker-item').forEach(btn => {
    btn.addEventListener('click', () => {
      const idx = parseInt(btn.dataset.idx);
      const sub = todoBlock.subtabs[idx];
      sub.name = text.slice(0, 30) + (text.length > 30 ? '...' : '');
      sub.items = items.map(t => ({
        id: crypto.randomUUID?.().slice(0, 8) || String(Date.now()),
        text: t, done: false
      }));
      State.update(() => {});
      Toast.show(`Чеклист: ${items.length} пунктов`, 'success');
      overlay.remove();
    });
  });
  overlay.querySelector('.checklist-picker-cancel')?.addEventListener('click', () => overlay.remove());
}
```

### 6. CSS для picker'а (`styles.css`)

```css
.checklist-picker-overlay {
  position: fixed; inset: 0; z-index: 10000;
  background: rgba(0,0,0,0.5); backdrop-filter: blur(4px);
  display: flex; align-items: center; justify-content: center;
  animation: fadeIn 0.15s ease;
}
.checklist-picker {
  background: var(--bg2); border: 1px solid var(--border2);
  border-radius: var(--radius); padding: 16px; min-width: 260px;
  box-shadow: var(--shadow-lg);
}
.checklist-picker-title { color: var(--text0); font-weight: 600; font-size: 13px; }
.checklist-picker-subtitle { color: var(--text2); font-size: 11px; margin-bottom: 10px; }
.checklist-picker-item {
  display: flex; justify-content: space-between; width: 100%;
  padding: 8px 10px; border: none; background: none;
  color: var(--text1); font-size: 12px; cursor: pointer;
  border-radius: var(--radius-sm); transition: background var(--trans);
}
.checklist-picker-item:hover { background: var(--surface2); color: var(--text0); }
.checklist-picker-count { color: var(--text3); font-size: 10px; }
.checklist-picker-cancel {
  width: 100%; margin-top: 8px; padding: 6px;
  border: 1px solid var(--border); background: none;
  color: var(--text2); font-size: 11px; border-radius: var(--radius-sm);
  cursor: pointer;
}
```

---

## Файлы для изменения

| Файл | Что менять |
|------|-----------|
| `llm-core.js` | Добавить `thesaurus_checklist` промпт в `BUILTIN_PROMPTS` и `PROMPT_META` |
| `llm-features.js` | Добавить `case 'checklist'` в `_executeThesaurusMode`, функцию `_thesaurusChecklistAtBlock`, `_showChecklistSubtabPicker` |
| `blocks.js` | Добавить `checklist: '+ чеклист'` в `_thesaurusModeLabels` и в массив меню |
| `styles.css` | CSS для `.checklist-picker-*` |

---

## Порядок реализации

1. `llm-core.js` — промпт
2. `blocks.js` — пункт меню
3. `llm-features.js` — обработчик + LLM вызов + парсинг + логика подвкладок
4. `styles.css` — picker
5. Git commit
6. Тест: выделить текст → долгий клик по тезаурусу → "+ чеклист" → проверить результат

---

## Верификация

1. Открыть проект, набрать текст в блоке
2. Выделить часть текста
3. Долгий клик по кнопке тезаурус → выбрать "+ чеклист"
4. Дождаться ответа LLM
5. Проверить что появился/обновился блок "Чеклист" с пунктами
6. Проверить что подвкладка названа по первым словам текста
7. Проверить сценарий когда все 5 подвкладок заняты — должен появиться picker
8. Проверить без выделения текста — должен взять весь блок
