Если ты забыл я напомню:
Ниже анализа файла проекта.

Анализатор не видел весь проект и в некоторых местах мог ошибаться, проведи расследование, что хорошо и полезно для нашего проекта, а что нет. Опасные и сомнительные улучшения пропускай. 
После применения патча,если нужно, пиши что исключить из следующего запроса, чтобы аудитор не искал эти проблемы.
Вместо списка на 100+ строк — компактная секция ## Исключить (уже исправлено в предыдущих раундах) и аудитор понимает **категории**, а не копает в деталях. Новые проблемы в этих же категориях он всё равно предложит, если они отличаются по существу.

--------------------


Я — Codex, coding agent на базе GPT-5.

### [1] [Критично] Потеря последнего ввода при закрытии из-за debounce history

**Проблема:** при закрытии блокнота последнее состояние текста может не попасть в историю. `_closeNotepad()` очищает `histTimer`, но затем вызывает `pushHistory`, а `pushHistory` игнорирует значение, если оно уже равно текущему `history[histIdx]`. Это ломает ожидаемый `undo` после повторного открытия/переключения вкладки: пользователь видит финальный текст, но стек истории может остаться без него.

**Строки:** [notepad.js](/workspace/notepad.js:199), [notepad.js](/workspace/notepad.js:313), [notepad.js](/workspace/notepad.js:555)

**Влияние:** теряется последнее редактирование в undo-истории, особенно если пользователь закрыл окно или быстро переключил вкладку раньше 600 мс.

**Патч:**
```diff
-    const ta = state.el?.querySelector('.notepad-body textarea');
-    if (ta) {
-      clearTimeout(state.histTimer);
-      state.tabs[state.activeTab].value = ta.value;
-      state._pushHistory?.(ta.value);
-    }
+    const ta = state.el?.querySelector('.notepad-body textarea');
+    if (ta) {
+      clearTimeout(state.histTimer);
+      state.tabs[state.activeTab].value = ta.value;
+      if (state.history[state.histIdx] !== ta.value) {
+        state._pushHistory?.(ta.value);
+      }
+    }
```

---

### [2] [Критично] История может стать неконсистентной после `MAX_HISTORY` trimming

**Проблема:** в `pushHistory()` после `shift()` индекс `histIdx` всегда ставится в `state.history.length - 1`, но не корректируется относительно удаления головы при наличии undo-ветки. Сейчас это обычно маскируется тем, что ветка redo отрезается раньше, но код хрупкий и легко ломается при любом локальном изменении порядка операций.

**Строки:** [notepad.js](/workspace/notepad.js:331)

**Влияние:** потенциально неверная навигация по истории, особенно в длинных сессиях редактирования.

**Патч:**
```diff
     function pushHistory(val) {
       if (state.histIdx < state.history.length - 1) {
         state.history.splice(state.histIdx + 1);
       }
       if (state.history[state.histIdx] === val) return;
       state.history.push(val);
-      if (state.history.length > MAX_HISTORY) state.history.shift();
+      if (state.history.length > MAX_HISTORY) {
+        state.history.shift();
+        state.histIdx = Math.max(0, state.histIdx - 1);
+      }
       state.histIdx = state.history.length - 1;
     }
```

---

### [3] [Критично] Асинхронный paste может вставить текст в уже другой textarea

**Проблема:** `_doPaste()` фокусирует textarea и асинхронно читает буфер. К моменту `then()` DOM может уже перерендериться, вкладка может смениться, textarea может стать другой. В `pasteBtn` проверяется только `activeTab` и `isConnected`, но не совпадение конкретного textarea.

**Строки:** [notepad.js](/workspace/notepad.js:139), [notepad.js](/workspace/notepad.js:397)

**Влияние:** вставка может попасть не в тот элемент после переключения состояния UI в коротком промежутке.

**Патч:**
```diff
     const pasteBtn = _mkBtn(SVG.paste, 'Вставить из буфера (или Ctrl+V)', () => {
       const ta = getTa(); if (!ta) return;
+      const taAtStart = ta;
       const tabAtStart = state.activeTab;
       _doPaste(ta, text => {
-        if (state.activeTab !== tabAtStart || !state.el?.isConnected) return;
+        if (state.activeTab !== tabAtStart || !state.el?.isConnected || getTa() !== taAtStart) return;
         pushHistory(ta.value);
         ta.setRangeText(text, ta.selectionStart, ta.selectionEnd, 'end');
         ta.dispatchEvent(new Event('input'));
       });
     });
```

---

### [4] [Критично] Асинхронный перевод может примениться к устаревшему selection

**Проблема:** `handleTranslate()` сохраняет `selStart/selEnd`, затем ждёт `translateProtected()`. Если пользователь изменил текст до завершения промиса, `setRangeText(result, selStart, selEnd, 'select')` заменит уже не тот диапазон.

**Строки:** [notepad.js](/workspace/notepad.js:462)

**Влияние:** тихая порча текста при быстром редактировании во время перевода.

**Патч:**
```diff
       const selStart = ta.selectionStart;
       const selEnd   = ta.selectionEnd;
       const tabAtStart = state.activeTab;
+      const sourceValue = ta.value;
       const hasSel   = selStart !== selEnd;
       const sel      = ta.value.substring(selStart, selEnd);
       const text     = hasSel ? sel : ta.value;
@@
       state._translateBusy = true;
       Translator.translateProtected(text, Translator.targetLang).then(result => {
         if (state.activeTab !== tabAtStart || !state.el?.isConnected) {
           _toast('Перевод отменён: вкладка изменена', 'info');
           return;
         }
+        if (ta.value !== sourceValue) {
+          _toast('Перевод отменён: текст изменён', 'info');
+          return;
+        }
         if (!result || result === text) { _toast('Не удалось перевести'); return; }
         pushHistory(ta.value);
```

---

### [5] [UX] `tabOffset` при создании берётся без дополнительной нормализации

**Проблема:** `_loadSaved()` нормализует `tabOffset`, но `create()` затем снова напрямую использует `saved?.tabOffset ?? 0`. Если `_loadSaved()` когда-нибудь начнёт возвращать частично доверенные данные, этот участок снова станет источником выхода за границы. Сейчас это не баг в рантайме, но уже дублирование ответственности.

**Строки:** [notepad.js](/workspace/notepad.js:182)

**Влияние:** поддержка осложняется; инварианты состояния размазаны по нескольким местам.

**Патч:**
```diff
-      tabOffset:    saved?.tabOffset ?? 0,
+      tabOffset:    Number.isInteger(saved?.tabOffset)
+        ? Math.max(0, Math.min(TAB_COUNT - VISIBLE_TABS, saved.tabOffset))
+        : 0,
```

---

### [6] [UX] Двойное срабатывание rename/title commit на `Enter` и `blur`

**Проблема:** и `commitTitle`, и `commitRename` вызываются по `Enter`, после чего input теряет фокус и приходит `blur`. Сейчас это частично гасится проверкой `display === 'none'`, но это неочевидная защита через DOM-состояние.

**Строки:** [notepad.js](/workspace/notepad.js:275), [notepad.js](/workspace/notepad.js:516)

**Влияние:** поведение хрупкое; любое изменение порядка display/focus может дать двойной `_persist()` и дублирующую логику.

**Патч:**
```diff
-    const commitTitle = () => {
+    let titleEditing = false;
+    titleLabel.ondblclick = e => {
+      e.stopPropagation();
+      titleEditing = true;
+      titleLabel.style.display = 'none';
+      titleInput.style.display = '';
+      titleInput.value = state.title;
+      titleInput.focus();
+      titleInput.select();
+    };
+    const commitTitle = () => {
+      if (!titleEditing) return;
+      titleEditing = false;
       if (titleInput.style.display === 'none') return;
       const v = titleInput.value.trim();
       if (v) state.title = v;
@@
-    titleLabel.ondblclick = e => {
-      e.stopPropagation();
-      titleLabel.style.display = 'none';
-      titleInput.style.display = '';
-      titleInput.value = state.title;
-      titleInput.focus();
-      titleInput.select();
-    };
```

И аналогично для rename вкладки:
```diff
-      const commitRename = () => {
+      let renameEditing = false;
+      const commitRename = () => {
+        if (!renameEditing) return;
+        renameEditing = false;
         if (renameInput.style.display === 'none') return;
         const v = renameInput.value.trim();
         if (v) state.tabs[i].label = v;
@@
       btn.ondblclick = e => {
         e.stopPropagation();
         clearTimeout(state.tabClickTimer);
         if (state.activeTab !== idx) {
           _switchTab(state, idx, state.el);
           _openRenameOnTab(state, idx);
         } else {
+          renameEditing = true;
           labelSpan.style.display = 'none';
           renameInput.style.display = '';
           renameInput.value = state.tabs[idx].label;
```

---

### [7] [UX] `Escape` в заголовке не восстанавливает исходное значение в input

**Проблема:** при редактировании title на `Escape` input скрывается, но его значение не откатывается. Визуально это не видно сразу, но при следующем открытии редактора пользователь получает старый незакоммиченный текст в поле до перезаписи `ondblclick`.

**Строки:** [notepad.js](/workspace/notepad.js:286)

**Влияние:** мелкая, но неприятная путаница в UX.

**Патч:**
```diff
-      if (e.key === 'Escape') { titleInput.style.display = 'none'; titleLabel.style.display = ''; }
+      if (e.key === 'Escape') {
+        titleInput.value = state.title;
+        titleInput.style.display = 'none';
+        titleLabel.style.display = '';
+      }
```

---

### [8] [UX] Вырезание без Clipboard API удаляет текст, хотя копирования не было

**Проблема:** fallback в `cutBtn` показывает сообщение "буфер недоступен, выделение удалено" и всё равно удаляет текст. Это нетипичное и рискованное поведение для cut: пользователь ожидает атомарность операции.

**Строки:** [notepad.js](/workspace/notepad.js:365)

**Влияние:** реальная потеря данных из выделения, если буфер недоступен.

**Патч:**
```diff
       if (navigator.clipboard?.writeText) {
         navigator.clipboard.writeText(sel)
           .then(removeSelection)
           .catch(() => _toast('Не удалось вырезать: буфер недоступен', 'error'));
       } else {
-        _toast('Буфер обмена недоступен, выделение удалено', 'info');
-        removeSelection();
+        _toast('Буфер обмена недоступен: вырезание отменено', 'error');
       }
```

---

### [9] [UX] Перевод не блокирует повторный клик на уровне кнопки

**Проблема:** логическая защита через `_translateBusy` есть, но UI не даёт визуального сигнала и позволяет спамить клики, создавая пачку toast-сообщений.

**Строки:** [notepad.js](/workspace/notepad.js:447)

**Влияние:** лишний шум и ощущение нестабильности интерфейса.

**Патч:**
```diff
-    const handleTranslate = () => {
+    const translateBtn = _mkBtn('<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" style="width:14px;height:14px"><circle cx="10" cy="10" r="7.5"/><path d="M2.5 10h15"/><path d="M10 2.5c2.5 2.5 3.5 5 3.5 7.5s-1 5-3.5 7.5"/><path d="M10 2.5c-2.5 2.5-3.5 5-3.5 7.5s1 5 3.5 7.5"/></svg>', 'Перевести текст');
+    const handleTranslate = () => {
       if (typeof Translator === 'undefined') { _toast('Модуль переводчика не загружен', 'error'); return; }
       const ta = getTa(); if (!ta) return;
       if (state._translateBusy) {
         _toast('Перевод уже выполняется...', 'info');
         return;
@@
       const lang = Translator.LANG_BY_CODE[Translator.targetLang];
       _toast('Перевод → ' + (lang?.name || Translator.targetLang) + '...');
       state._translateBusy = true;
+      translateBtn.disabled = true;
       Translator.translateProtected(text, Translator.targetLang).then(result => {
@@
-      }).catch(err => _toast('Ошибка: ' + err.message))
-        .finally(() => { state._translateBusy = false; });
+      }).catch(err => _toast('Ошибка: ' + err.message))
+        .finally(() => {
+          state._translateBusy = false;
+          translateBtn.disabled = false;
+        });
     };
+    translateBtn.addEventListener('click', handleTranslate);
@@
-      _mkBtn('<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" style="width:14px;height:14px"><circle cx="10" cy="10" r="7.5"/><path d="M2.5 10h15"/><path d="M10 2.5c2.5 2.5 3.5 5 3.5 7.5s-1 5-3.5 7.5"/><path d="M10 2.5c-2.5 2.5-3.5 5-3.5 7.5s1 5 3.5 7.5"/></svg>', 'Перевести текст', handleTranslate),
+      translateBtn,
```

---

### [10] [Производительность] `_persist()` сериализует всё состояние слишком часто

**Проблема:** даже при `_lastPersistPayload` выполняется полный `JSON.stringify(...)` на каждый вызов `_persist()`. А вызовов много: undo/redo, clear, switchTab, resize, drag, toggle minimize. При больших текстах стоимость заметная.

**Строки:** [notepad.js](/workspace/notepad.js:39)

**Влияние:** лишняя нагрузка на main thread при больших вкладках.

**Патч:**
```diff
   function _persist(state) {
     try {
-      const payload = JSON.stringify({
+      const snapshot = {
         title:     state.title,
         tabs:      state.tabs.map(tab => ({
           label: tab.label,
           value: tab.value,
         })),
         activeTab: state.activeTab,
         tabOffset: state.tabOffset,
         fontSize:  state.fontSize,
         minimized: state.minimized,
         pos:       state.pos,
         size:      state.size,
-      });
+      };
+      if (
+        _lastPersistPayload &&
+        state._lastPersistRef &&
+        state._lastPersistRef.title === snapshot.title &&
+        state._lastPersistRef.activeTab === snapshot.activeTab &&
+        state._lastPersistRef.tabOffset === snapshot.tabOffset &&
+        state._lastPersistRef.fontSize === snapshot.fontSize &&
+        state._lastPersistRef.minimized === snapshot.minimized &&
+        JSON.stringify(state._lastPersistRef.pos) === JSON.stringify(snapshot.pos) &&
+        JSON.stringify(state._lastPersistRef.size) === JSON.stringify(snapshot.size) &&
+        state._lastPersistRef.tabs.length === snapshot.tabs.length &&
+        state._lastPersistRef.tabs.every((tab, i) =>
+          tab.label === snapshot.tabs[i].label && tab.value === snapshot.tabs[i].value
+        )
+      ) return;
+      const payload = JSON.stringify(snapshot);
       if (payload === _lastPersistPayload) return;
       localStorage.setItem(STORE_KEY, payload);
       _lastPersistPayload = payload;
+      state._lastPersistRef = snapshot;
```

Примечание: это самостоятельный патч, но он добавляет служебное поле в runtime-state без изменения публичного API.

---

### [11] [Производительность] Полный re-render табов на каждый undo/redo и часть input-сценариев

**Проблема:** `doUndo()`/`doRedo()` всегда вызывают `_renderTabs(state)`, хотя визуально нужно обновлять вкладки только при смене признака `filled` или active state. Для undo/redo active state не меняется.

**Строки:** [notepad.js](/workspace/notepad.js:344), [notepad.js](/workspace/notepad.js:355)

**Влияние:** лишнее пересоздание DOM вкладок при интенсивной работе с историей.

**Патч:**
```diff
     const doUndo = () => {
       const ta = getTa();
       if (!ta || state.histIdx <= 0) return;
+      const wasFilled = !!state.tabs[state.activeTab].value;
       state.histIdx--;
       ta.value = state.history[state.histIdx];
       ta.selectionStart = ta.selectionEnd = ta.value.length;
       state.tabs[state.activeTab].value = ta.value;
       _updateCount(ta, countSpan);
-      _renderTabs(state);
+      if (wasFilled !== !!ta.value) _renderTabs(state);
       _persist(state);
     };
@@
     const doRedo = () => {
       const ta = getTa();
       if (!ta || state.histIdx >= state.history.length - 1) return;
+      const wasFilled = !!state.tabs[state.activeTab].value;
       state.histIdx++;
       ta.value = state.history[state.histIdx];
       ta.selectionStart = ta.selectionEnd = ta.value.length;
       state.tabs[state.activeTab].value = ta.value;
       _updateCount(ta, countSpan);
-      _renderTabs(state);
+      if (wasFilled !== !!ta.value) _renderTabs(state);
       _persist(state);
     };
```

---

### [12] [Читаемость] `_switchTab()` хранит историю вкладки в `tabs[idx]._history` и `_histIdx` как скрытые поля доменной модели

**Проблема:** данные UI-history подмешиваются прямо в persisted tab-объекты. Сейчас это не утекает в localStorage, потому что `_persist()` сериализует только `label/value`, но структура `tabs` становится неочевидной.

**Строки:** [notepad.js](/workspace/notepad.js:557)

**Влияние:** код сложнее сопровождать; легко ошибиться при будущих изменениях persistence.

**Патч:**
```diff
-      state.tabs[state.activeTab]._history = state.history.slice();
-      state.tabs[state.activeTab]._histIdx = state.histIdx;
+      state.tabs[state.activeTab].historyState = {
+        list: state.history.slice(),
+        idx: state.histIdx,
+      };
@@
-      state.history = state.tabs[idx]._history || [newVal];
-      state.histIdx = state.tabs[idx]._histIdx ?? state.history.length - 1;
+      state.history = state.tabs[idx].historyState?.list || [newVal];
+      state.histIdx = state.tabs[idx].historyState?.idx ?? state.history.length - 1;
```

Это не меняет поведение, но делает смысл данных явным.

---

### [13] [Читаемость] `create()` содержит дублирующий cleanup для “зависшего” `_instance`

**Проблема:** ветка `if (_instance && !_instance.el?.isConnected)` вручную повторяет часть `_closeNotepad()`-логики, но не полностью. Это уже источник расхождений: тут нет `document.body.style.userSelect = ''`, нет `tabClickTimer`, нет финализации текста активной вкладки через textarea.

**Строки:** [notepad.js](/workspace/notepad.js:165)

**Влияние:** cleanup размазан, сложнее гарантировать одинаковое поведение.

**Патч:**
```diff
     if (_instance && !_instance.el?.isConnected) {
-      clearTimeout(_instance.histTimer);
-      _persist(_instance);
-      _instance.dragAbort?.abort();
-      _instance.resizeAbort?.abort();
+      clearTimeout(_instance.histTimer);
+      clearTimeout(_instance.tabClickTimer);
+      _instance.dragAbort?.abort();
+      _instance.resizeAbort?.abort();
+      document.body.style.userSelect = '';
+      _persist(_instance);
       _instance = null;
     }
```

---

### [14] [UX] Drag boundary разрешает полностью увести окно вниз/вправо за предел видимой рабочей зоны

**Проблема:** ограничение только `0..window.innerWidth - el.offsetWidth` / `0..window.innerHeight - el.offsetHeight`. Для крупного окна это нормально, но для обычного сценария шапка может оказаться почти недоступной на маленьких экранах после resize + drag.

**Строки:** [notepad.js](/workspace/notepad.js:738)

**Влияние:** окно можно разместить так, что его трудно “схватить” снова.

**Патч:**
```diff
       document.addEventListener('mousemove', mv => {
-        const maxLeft = Math.max(0, window.innerWidth  - el.offsetWidth);
-        const maxTop  = Math.max(0, window.innerHeight - el.offsetHeight);
-        el.style.left = Math.max(0, Math.min(maxLeft, startL + (mv.clientX - startX))) + 'px';
-        el.style.top  = Math.max(0, Math.min(maxTop,  startT + (mv.clientY - startY))) + 'px';
+        const visibleHeader = 48;
+        const maxLeft = Math.max(0, window.innerWidth  - visibleHeader);
+        const maxTop  = Math.max(0, window.innerHeight - visibleHeader);
+        el.style.left = Math.max(-(el.offsetWidth - visibleHeader), Math.min(maxLeft, startL + (mv.clientX - startX))) + 'px';
+        el.style.top  = Math.max(0, Math.min(maxTop,  startT + (mv.clientY - startY))) + 'px';
       }, { signal: ac.signal });
```

---

### [15] [UX] `Ctrl+S` уже реализован, но tooltip save-кнопки не подсказывает это

**Проблема:** горячая клавиша есть, но интерфейс не сигнализирует о ней. Пользователь не узнает о поведении без чтения кода.

**Строки:** [notepad.js](/workspace/notepad.js:420), [notepad.js](/workspace/notepad.js:655)

**Влияние:** потерянная discoverability полезной функции.

**Патч:**
```diff
-    const saveBtn = _mkBtn(SVG.save, 'Сохранить в .txt', saveToFile);
+    const saveBtn = _mkBtn(SVG.save, 'Сохранить в .txt (Ctrl+S)', saveToFile);
```

---

Краткая сводка: 4 критических, 7 UX, 2 производительность, 2 читаемость. В целом код качественный и аккуратно собран для vanilla JS/IIFE: хорошая изоляция state, нормальный cleanup через `AbortController`, разумная защита от XSS в пользовательских строках. Основные проблемы сосредоточены в асинхронных сценариях (`paste`, `translate`) и в тонкостях undo/history под debounce.