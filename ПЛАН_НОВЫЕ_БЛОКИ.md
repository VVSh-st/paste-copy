
# ПЛАН: Sticky Note + TODO + IndexedDB Backup

## Контекст проекта

paste\copy — браузерный конструктор LLM-промптов. Vanilla JS, без фреймворков.
Данные в localStorage, синхронизация через GitHub Gist.

Текущие типы блоков: text (textarea + 20 подвкладок), snippets (список с чекбоксами),
commands (список команд), group (контейнер), variable (пары name=value).

Архитектура блоков:
- renderBlock() в blocks.js определяет тип и вызывает render*Body()
- Каждый блок имеет заголовок (createHeader), тело и drag-and-drop
- Превью (Preview.render() в ui.js) агрегирует текст из блоков в итоговый промпт
- Блоки хранятся в tab.blocks[], состояния в localStorage через Storage.js

---

## ФАЗА 1: Sticky Note (Блок-заметка)

### 1.1 Фабрика блока (ui.js)

Добавить `makeSticky(col)` рядом с makeTextBlock/makeSnippets:

```js
function makeSticky(col) {
  return {
    id: uid(), type: 'sticky', title: 'Заметка', icon: '📌', column: col,
    collapsed: false,
    color: 'yellow',  // yellow | green | blue | pink | gray
    value: '',
    fontSize: 13,
  };
}
```

### 1.2 Рендер тела (blocks.js)

В renderBlock() добавить case:
```js
else if (b.type === 'sticky') renderStickyBody(b, body);
```

Функция renderStickyBody():
- Один textarea (без подвкладок)
- placeholder: «Личная заметка, не попадёт в промпт»
- Auto-grow: textarea растёт с текстом (max-height: 400px, overflow: auto)
- Под textarea: серый лейбл «private» (маленький, muted)
- НЕТ undo/redo/translate/lint кнопок — только textarea

### 1.3 Кнопка цвета в заголовке

В createHeader(), после иконки типа, добавить палитру для sticky:
- Кнопка 🎨 (или цветной квадрат 12x12px)
- Клик → инлайн-ряд из 5 кружков под заголовком
- Клик по кружку → b.color = ..., закрытие палитры
- 5 цветов: yellow, green, blue, pink, gray

### 1.4 CSS (styles.css)

```css
/* Sticky Note */
.block-type-sticky { border-left: 3px solid var(--note-color, var(--accent)); }
.block-type-sticky .block-header {
  background: color-mix(in srgb, var(--note-color, #d4c373) 18%, var(--bg-block));
}
.block-type-sticky .block-textarea {
  background: color-mix(in srgb, var(--note-color, #d4c373) 6%, var(--bg-block));
  min-height: 60px;
  resize: vertical;
}
.block-type-sticky .note-private {
  font-size: 10px; color: var(--text-muted); text-align: right;
  padding: 2px 8px; opacity: 0.5;
}

/* Цветовые темы */
.block-type-sticky[data-color="yellow"] { --note-color: #d4c373; }
.block-type-sticky[data-color="green"]  { --note-color: #7fb389; }
.block-type-sticky[data-color="blue"]   { --note-color: #7aa7d4; }
.block-type-sticky[data-color="pink"]   { --note-color: #cf8fb3; }
.block-type-sticky[data-color="gray"]   { --note-color: #9aa0a6; }

/* Палитра цветов */
.color-palette { display: flex; gap: 4px; padding: 4px 8px; }
.color-dot {
  width: 14px; height: 14px; border-radius: 50%; cursor: pointer;
  border: 2px solid transparent; transition: border-color 0.15s;
}
.color-dot:hover, .color-dot.active { border-color: var(--text); }
```

### 1.5 Превью (ui.js — Preview.render)

В цикле по блокам добавить проверку:
```js
if (b.type === 'sticky') continue;  // пропускаем, не в превью
```

### 1.6 Свёрнутый вид

В renderBlock(), если b.collapsed && b.type === 'sticky':
- Показать: 📝 Заголовок · «первая строка текста» [цветной квадрат]
- Цветной квадрат 8x8px в конце строки

### 1.7 Сериализация

В state.js migrate() добавить дефолт для sticky:
```js
if (b.type === 'sticky') {
  if (b.color === undefined) b.color = 'yellow';
}
```

---

## ФАЗА 2: TODO (Чеклист)

### 2.1 Фабрика блока (ui.js)

```js
function makeTodo(col) {
  return {
    id: uid(), type: 'todo', title: 'Чеклист', icon: '☑️', column: col,
    collapsed: false,
    activeSubtab: 0,
    subtabs: Array.from({ length: 10 }, (_, i) => ({
      label: String(i + 1), name: '',
      items: [],
    })),
  };
}
```

### 2.2 Структура данных подвкладки

```js
subtabs[i] = {
  label: '1',
  name: 'Имя списка',     // опциональное имя
  items: [
    { id: 'abc123', text: 'Купить молоко', done: false },
    { id: 'def456', text: 'Написать промпт', done: true },
  ]
}
```

### 2.3 Рендер тела (blocks.js)

В renderBlock() добавить case:
```js
else if (b.type === 'todo') renderTodoBody(b, body);
```

renderTodoBody() включает:
1. Строка подвкладок (переиспользовать createSubtabNav или аналог)
2. Список пунктов текущей подвкладки
3. Кнопка «+ Добавить пункт»

Каждый пункт (.todo-item):
- drag-handle ⠿ (hover-visible)
- чекбокс ☐/☑
- текст (input/contenteditable)
- кнопка × удалить

### 2.4 Чекбокс поведение

- Клик по чекбоксу → item.done = !item.done
- Выполненный: opacity: 0.55, text-decoration: line-through
- Счётчик в заголовке: doneCount / totalCount по текущей подвкладке
- Если все выполнены: зелёный цвет счётчика + ✓

### 2.5 Горячие клавиши

- Enter → создать новый пункт ниже
- Enter на пустом → удалить пустой, выйти
- Backspace в начале пустого → удалить, фокус на предыдущий
- Shift+Enter → перенос строки внутри пункта
- Ctrl+↑/↓ → перемещение пункта вверх/вниз

### 2.6 Превью

```js
// В Preview.render(), для типа 'todo':
if (b.type === 'todo') {
  const sub = b.subtabs[b.activeSubtab];
  if (!sub || !sub.items.length) continue;
  const lines = sub.items
    .filter(it => it.text.trim())
    .map(it => `- [${it.done ? 'x' : ' '}] ${it.text}`);
  if (lines.length) {
    // добавить в превью с заголовком если есть name
    if (sub.name) result.push(`## ${sub.name}`);
    result.push(...lines);
  }
}
```

### 2.7 CSS

```css
.block-type-todo .todo-list { padding: 4px 0; }

.todo-item {
  display: flex; align-items: center; gap: 6px;
  padding: 3px 8px; border-radius: 4px;
}
.todo-item:hover { background: var(--hover-bg); }
.todo-item.dragging { opacity: 0.4; }

.todo-handle {
  cursor: grab; color: var(--text-muted); opacity: 0;
  transition: opacity 0.15s; font-size: 12px;
}
.todo-item:hover .todo-handle { opacity: 0.6; }

.todo-checkbox {
  width: 16px; height: 16px; cursor: pointer;
  accent-color: var(--accent);
}

.todo-text {
  flex: 1; border: none; background: transparent;
  color: var(--text); font-size: inherit; outline: none;
}
.todo-text.done {
  opacity: 0.55; text-decoration: line-through;
}

.todo-delete {
  opacity: 0; background: none; border: none;
  color: var(--text-muted); cursor: pointer;
}
.todo-item:hover .todo-delete { opacity: 0.5; }

.todo-empty {
  padding: 16px; text-align: center;
  color: var(--text-muted); font-size: 12px;
}
```

### 2.8 Счётчик в заголовке

В createHeader(), после иконки:
```js
if (b.type === 'todo') {
  const sub = b.subtabs[b.activeSubtab];
  const items = sub?.items || [];
  const done = items.filter(i => i.done).length;
  const total = items.length;
  const badge = document.createElement('span');
  badge.className = 'block-todo-count' + (done === total && total ? ' all-done' : '');
  badge.textContent = `${done}/${total}`;
  h.appendChild(badge);
}
```

---

## ФАЗА 3: IndexedDB Backup (в GistSync)

### 3.1 Модуль local-backup.js (~150 строк)

```js
const LocalBackup = (() => {
  const DB_NAME = 'paste-copy-backup';
  const STORE = 'snapshots';
  const MAX_BACKUPS = 30;
  let db = null;

  function init() {
    return new Promise((resolve, reject) => {
      const req = indexedDB.open(DB_NAME, 1);
      req.onupgradeneeded = e => {
        const d = e.target.result;
        if (!d.objectStoreNames.contains(STORE)) {
          d.createObjectStore(STORE, { keyPath: 'ts' });
        }
      };
      req.onsuccess = e => { db = e.target.result; resolve(db); };
      req.onerror = () => reject(req.error);
    });
  }

  async function save(state) {
    if (!db) await init();
    const data = JSON.stringify(state);
    const entry = {
      ts: Date.now(),
      size: data.length,
      tabsCount: state.tabs?.length ?? 0,
      data,
    };
    return new Promise((resolve, reject) => {
      const tx = db.transaction(STORE, 'readwrite');
      tx.objectStore(STORE).put(entry);
      tx.oncomplete = async () => {
        await prune();
        resolve();
      };
      tx.onerror = () => reject(tx.error);
    });
  }

  async function list() {
    if (!db) await init();
    return new Promise((resolve, reject) => {
      const tx = db.transaction(STORE, 'readonly');
      const req = tx.objectStore(STORE).getAll();
      req.onsuccess = () => {
        const items = req.result.sort((a, b) => b.ts - a.ts);
        resolve(items);
      };
      req.onerror = () => reject(req.error);
    });
  }

  async function restore(ts) {
    if (!db) await init();
    return new Promise((resolve, reject) => {
      const tx = db.transaction(STORE, 'readonly');
      const req = tx.objectStore(STORE).get(ts);
      req.onsuccess = () => resolve(req.result?.data ? JSON.parse(req.result.data) : null);
      req.onerror = () => reject(req.error);
    });
  }

  async function prune() {
    const items = await list();
    if (items.length <= MAX_BACKUPS) return;
    const toDelete = items.slice(MAX_BACKUPS);
    const tx = db.transaction(STORE, 'readwrite');
    const store = tx.objectStore(STORE);
    toDelete.forEach(item => store.delete(item.ts));
  }

  async function clear() {
    if (!db) await init();
    return new Promise((resolve, reject) => {
      const tx = db.transaction(STORE, 'readwrite');
      tx.objectStore(STORE).clear();
      tx.oncomplete = () => resolve();
      tx.onerror = () => reject(tx.error);
    });
  }

  return { init, save, list, restore, prune, clear };
})();
```

### 3.2 Интеграция с GistSync (gist-sync.js)

В _pushImpl(), после успешного push в Gist, добавить:
```js
// IndexedDB backup
try { await LocalBackup.save(state); } catch (e) { console.warn('LocalBackup failed:', e); }
```

### 3.3 Вкладка «Локальные копии» в модалке Gist

В gist-sync.js, в функции открытия модалки, добавить третью вкладку:

```
[Sync] [История] [Локальные копии]
```

При переключении на «Локальные копии»:
1. Вызвать LocalBackup.list()
2. Отрисовать список копий
3. Кнопки: ↺ Восстановить, ⬇ Скачать
4. Кнопка «Очистить все» вверху

UI копии:
```
● Сегодня 14:32  ·  4 вкладки  ·  38 KB    [↺][⬇]
○ Сегодня 11:05  ·  4 вкладки  ·  37 KB    [↺][⬇]
○ Вчера 18:40    ·  3 вкладки  ·  35 KB    [↺][⬇]
```

### 3.4 Восстановление с подтверждением

При нажатии «Восстановить»:
1. Сначала сохранить текущее состояние (safety snapshot)
2. Показать модалку: «Восстановить копию от [дата]?\nТекущее состояние будет сохранено автоматически.»
3. [Отмена] [Восстановить]
4. После восстановления: State.load(restoredData), fullRender()

### 3.5 CSS

```css
.backup-list { max-height: 400px; overflow-y: auto; }
.backup-item {
  display: flex; align-items: center; justify-content: space-between;
  padding: 8px 12px; border-radius: 6px; gap: 12px;
}
.backup-item:hover { background: var(--hover-bg); }
.backup-meta { font-size: 12px; color: var(--text-muted); }
.backup-actions { display: flex; gap: 4px; }
.backup-empty {
  padding: 32px; text-align: center; color: var(--text-muted);
}
```

---

## ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ

### Новые файлы:
- `local-backup.js` — IndexedDB модуль (~100 строк)
- `ПЛАН_НОВЫЕ_БЛОКИ.md` — этот файл

### Изменяемые файлы:
- `blocks.js` — renderStickyBody(), renderTodoBody(), case в renderBlock()
- `ui.js` — makeSticky(), makeTodo(), BUILTIN шаблоны, Preview.render() пропуск sticky
- `state.js` — migrate() для sticky.color и todo.subtabs
- `gist-sync.js` — LocalBackup.save() после push, вкладка Backups в модалке
- `index.html` — <script src="local-backup.js"> перед gist-sync.js
- `styles.css` — стили .block-type-sticky, .block-type-todo, .backup-*

### Порядок реализации:
1. local-backup.js (независимый модуль, можно тестировать отдельно)
2. Sticky Note (самый простой, быстрый win)
3. TODO (самый сложный, требует подвкладки + чекбоксы + drag)
4. Интеграция IndexedDB в GistSync (после local-backup.js готов)
5. Вкладка Backups в модалке (после интеграции)

---
