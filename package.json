{
  "name": "cherry-studio-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python -m http.server 8080",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
